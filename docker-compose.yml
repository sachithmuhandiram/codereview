version: '3'

services:
  apigateway:
    image: apigateway
    build:
      context: .
      dockerfile: Dockerfile
    network_mode: "host"
    
  notification:
    image: notification
    build:
      context: ./notification
      dockerfile: Dockerfile
    network_mode: "host"

  user:
    image: user
    build:
      context: ./user
      dockerfile: Dockerfile
    network_mode: "host"
